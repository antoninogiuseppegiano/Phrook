@page
@model GenerateRecoveryCodesModel
@{
    ViewData["Title"] = "Genera codici di recupero per l'autenticazione a due fattori (2FA)";@* Generate two-factor authentication (2FA) recovery codes *@
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />
<h4>@ViewData["Title"]</h4>
<div class="alert alert-warning" role="alert">
    <p>
        <span class="glyphicon glyphicon-warning-sign"></span>
        <strong>Conserva questi codici in un posto sicuro.</strong>@* Put these codes in a safe place. *@
    </p>
    <p>
        @* If you lose your device and don't have the recovery codes you will lose access to your account. *@
		Se perdi l'accesso all'app di autenticazione e non disponi dei codici di recupero, perderai l'accesso al tuo account.
    </p>
    <p>
        @* Generating new recovery codes does not change the keys used in authenticator apps. If you wish to change the key
        used in an authenticator app you should <a asp-page="./ResetAuthenticator">reset your authenticator keys.</a> *@
		La generazione di nuovi codici di recupero non modifica le chiavi utilizzate nelle app di autenticazione. 
		Se desideri modificare la chiave utilizzata in un'app di autenticazione, devi <a asp-page="./ResetAuthenticator"></a>reimpostare le chiavi di autenticazione</a>.
    </p>
</div>
<div>
    <form method="post" class="form-group">
        <button class="btn btn-danger" type="submit">Genera codici di recupero</button>@* Generate Recovery Codes *@
    </form>
</div>