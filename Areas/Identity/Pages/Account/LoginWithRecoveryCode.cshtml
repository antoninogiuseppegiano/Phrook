@page
@model LoginWithRecoveryCodeModel
@{
    ViewData["Title"] = "Verifica del codice di recupero";@* Recovery code verification *@
}

<h1>@ViewData["Title"]</h1>
<hr />
<p>
    @* You have requested to log in with a recovery code. This login will not be remembered until you provide
    an authenticator app code at log in or disable 2FA and log in again. *@
	Hai richiesto di accedere con un codice di recupero. 
	Questo accesso non verrà ricordato fino a quando non fornirai all'accesso un codice per l'app di autenticazione o finché non disabiliterai 2FA e non accederai di nuovo.
</p>
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Input.RecoveryCode"></label>
                <input asp-for="Input.RecoveryCode" class="form-control" autocomplete="off" />
                <span asp-validation-for="Input.RecoveryCode" class="text-danger"></span>
            </div>
            <button type="submit" class="btn btn-primary">Accedi</button>@* Log in *@
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
